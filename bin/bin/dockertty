#!/bin/bash

if [ -z "$1" ]; then
    selected=$(podman ps --format '{{ .Names }}' | fzf)
    if [ -n "$selected" ]; then
        echo "$selected"
        podman exec -it "$selected" bash
    fi
else
    podman exec -it "$1" bash
fi
